/*
短代码调试，测试部分功能可行性
*/

console.show()
console.print("开始")
var d8list = depth(8).clickable(true).find()
for (var index = a.length - 1; index >= 0; index--) {
    console.print(a[index])
    a[index].click()
}